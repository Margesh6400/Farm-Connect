import React from 'react';

const CreateContract = () => (
  <div>
    <h2 className="text-2xl font-semibold text-green-900">Create a New Contract</h2>
    <p className="mt-4 text-lg text-green-600">Initiate a contract by selecting a buyer and defining the terms.</p>
    {/* Add form for creating a new contract */}
  </div>
);

export default CreateContract;
